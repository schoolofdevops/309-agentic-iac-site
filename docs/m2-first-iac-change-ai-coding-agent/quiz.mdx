---
sidebar_position: 5
title: 'Quiz: Build Your First IaC Change with an AI Coding Agent'
---

import Quiz from '@site/src/components/Quiz';

# Build Your First IaC Change with an AI Coding Agent Quiz

<Quiz questions={[
  {
    prompt:'Why is “fix the Terraform” not a complete agent task?',options:[{text:'It omits the objective boundary, allowed actions, evidence, and stop conditions',correct:true,explanation:'A concern becomes executable only when the expected result and control boundaries are explicit.'},{text:'It does not identify the coding-agent product, model version, and vendor that must execute every step',correct:false,explanation:'A portable task can work across compatible coding-agent interfaces.'},{text:'It is too short for a language model',correct:false,explanation:'Prompt length does not create an engineering contract.'}]},
  {
    prompt:'Which item belongs in the objective of the Section 2 repair?',options:[{text:'Allow the agent to edit any Terraform file it discovers',correct:false,explanation:'That is an unsafe work boundary, not an objective.'},{text:'Restore the missing resource while keeping the output backed by its generated value',correct:true,explanation:'The objective states the observable behaviour that must be restored.'},{text:'Run apply after the first green check',correct:false,explanation:'Apply is forbidden and is outside the repair objective.'}]},
  {
    prompt:'Select all controls that belong in a bounded IaC task contract.',multiSelect:true,options:[{text:'Named allowed files',correct:true,explanation:'The file boundary limits the change surface.'},{text:'Concrete forbidden actions',correct:true,explanation:'Explicit verbs make unsafe actions reviewable and enforceable.'},{text:'Required evidence and stop conditions',correct:true,explanation:'They define proof and when authority returns to a human.'},{text:'Permission to widen scope when convenient',correct:false,explanation:'A wider scope requires a new decision, not agent convenience.'}]},
  {
    prompt:'Why are file boundaries and tool boundaries both required?',options:[{text:'Together they make every Terraform plan safe to apply after one successful configuration check',correct:false,explanation:'A plan still needs environment, policy, approval, and replacement review.'},{text:'They remove the need for a separate human checkpoint before delivery',correct:false,explanation:'Risk-changing actions still need an explicit decision.'},{text:'They limit different capabilities: what can change and what actions can run',correct:true,explanation:'A narrow file scope does not make a state-changing tool safe, and read-only tools do not limit edits.'}]},
  {
    prompt:'What should an agent inspect before editing an IaC module?',options:[{text:'Only the source line named in the latest error message',correct:false,explanation:'The error needs repository, provider, and state context.'},{text:'Repository rules, affected files, state boundary, current failure, and working tree',correct:true,explanation:'These observations establish the current scope and preserve existing work.'},{text:'Production credentials, remote state contents, and every reachable live resource by default',correct:false,explanation:'Access must be explicitly required and authorized.'}]},
  {
    prompt:'Which statement is a fact rather than an assumption?',options:[{text:'Four bytes is always the correct identifier size',correct:false,explanation:'The size must come from the task or another trusted requirement.'},{text:'One lock file proves both engines are compatible',correct:false,explanation:'Terraform and OpenTofu can use different provider sources and hashes.'},{text:'The starter output references random_id.platform.hex',correct:true,explanation:'The statement can be verified directly in the current file.'}]},
  {
    prompt:'What does a plan prove before the agent edits?',options:[{text:'That the final configuration is valid in both supported engines and safe in the target environment',correct:false,explanation:'Only execution and independent checks can support that claim.'},{text:'The intended path, affected scope, tools, checks, and stopping point',correct:true,explanation:'A plan exposes intent for review before action.'},{text:'That apply has been approved for the target environment',correct:false,explanation:'Apply needs its own explicit authorization.'}]},
  {
    prompt:'In the governed state machine, why is there no transition to apply?',options:[{text:'Terraform cannot create a random resource in a local module',correct:false,explanation:'Terraform can apply it, but the course task forbids doing so.'},{text:'Validation automatically creates the resource without state',correct:false,explanation:'Validation checks configuration and does not create infrastructure.'},{text:'Apply is not an authorized action in this repair task',correct:true,explanation:'An unavailable transition prevents the workflow from silently changing risk class.'}]},
  {
    prompt:'When should a bounded agent stop instead of retrying?',options:[{text:'Whenever a formatter returns no text after a successful check in the approved workspace',correct:false,explanation:'A silent formatter with exit zero is a normal success result.'},{text:'When another file, credential, or forbidden action is required',correct:true,explanation:'The task no longer has enough authority to continue safely.'},{text:'Only after it has attempted an apply to discover more errors',correct:false,explanation:'Apply must never be used as a repair diagnostic here.'}]},
  {
    prompt:'What does terraform fmt -check with exit status 0 prove?',options:[{text:'The checked Terraform file follows formatter rules',correct:true,explanation:'Formatting is a narrow style claim.'},{text:'All resource references resolve correctly',correct:false,explanation:'Semantic validation is a separate check.'},{text:'The change is safe to deploy',correct:false,explanation:'Formatting says nothing about runtime effects or policy.'}]},
  {
    prompt:'Which evidence best supports the claim that only the allowed tracked file changed?',options:[{text:'A successful provider download from the configured registry',correct:false,explanation:'Initialization does not report the repository change scope.'},{text:'An agent summary that describes the proposal as a small fix',correct:false,explanation:'A natural-language claim does not expose the changed paths.'},{text:'The changed-file list together with the exact Git diff',correct:true,explanation:'The list checks scope and the diff shows the proposed content.'}]},
  {
    prompt:'Codex edits the correct file but its sandbox cannot resolve the provider registry. What is the accurate conclusion?',options:[{text:'Codex completed every required validation inside its workspace sandbox',correct:false,explanation:'The sandbox stopped before those checks could complete.'},{text:'The scoped repair was authored, but validation still needs an independent runner with registry access',correct:true,explanation:'The code change and the validation proof come from distinct evidence records.'},{text:'The agent should request cloud administrator credentials and broader network access to repair registry DNS',correct:false,explanation:'Credentials do not fix DNS and are forbidden by the task.'}]},
  {
    prompt:'OpenTofu rewrites the disposable lock entry from the Terraform registry to the OpenTofu registry. What should the reviewer conclude?',options:[{text:'The repair failed because both engines must produce byte-identical provider locks for every platform',correct:false,explanation:'The fixture expects the source rewrite and ignores its disposable lock.'},{text:'Commit the rewritten lock as proof that every provider is portable',correct:false,explanation:'A single rewritten lock does not prove both tool paths.'},{text:'Record the warning and keep separate validation; the shared lock is not compatibility proof',correct:true,explanation:'Provider sources and release hashes can differ even when the selected version is preserved.'}]},
  {
    prompt:'An agent proposes terraform apply -auto-approve after validation. What is the first recovery action?',options:[{text:'Approve the command because every local validation has passed in the isolated worktree',correct:false,explanation:'Validation is not deployment approval.'},{text:'Reject the proposal and name apply as outside the authorized boundary',correct:true,explanation:'The workflow returns to the last trusted checkpoint without increasing authority.'},{text:'Give the agent credentials but remove only the auto-approve flag',correct:false,explanation:'Any apply and credential access remain outside this task.'}]},
  {
    prompt:'Which bundle is sufficient for the Section 2 checkpoint?',options:[{text:'A correct HCL snippet plus the agent summary copied from chat',correct:false,explanation:'A snippet lacks repository scope and executed checks.'},{text:'One successful Terraform validation line from an unknown checkout',correct:false,explanation:'It omits formatting, initialization, OpenTofu, diff scope, and the stop record.'},{text:'Task contract, one-file diff, both tool checks, lock warning if present, and a no-apply stop',correct:true,explanation:'The bundle links scope, cross-tool validation, known limits, and the stopping boundary.'}]}
]} />
