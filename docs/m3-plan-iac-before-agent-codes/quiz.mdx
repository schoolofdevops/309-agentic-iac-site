---
sidebar_position: 5
title: 'Quiz: Plan Your IaC Change Before the Agent Writes Code'
---

import Quiz from '@site/src/components/Quiz';

# Plan Your IaC Change Before the Agent Writes Code Quiz

<Quiz questions={[
  {
    prompt:'A product owner says, “Add asynchronous processing.” Which first rewrite gives an implementation agent the strongest starting point?',options:[{text:'Create an SQS-compatible queue named prod-ai-jobs with the newest provider and let generated code define the remaining behaviour',correct:false,explanation:'This selects technology and naming before observable behaviour and constraints are agreed.'},{text:'Return HTTP 202 and a non-empty job ID within 500 milliseconds for a valid local test request',correct:true,explanation:'This states an observable result that a future test can verify without forcing one implementation.'},{text:'Generate Terraform, Helm, GitOps, and application files first, then ask reviewers which behaviours the completed feature should keep',correct:false,explanation:'Generated code should not make the missing product and ownership decisions.'}]},
  {
    prompt:'Select all statements that belong in the queue change brief as acceptance criteria.',multiSelect:true,options:[{text:'Local, test, and production use separate state and queue resources',correct:true,explanation:'Environment isolation is an observable design condition with reviewable evidence.'},{text:'The API reports queued, running, succeeded, or failed for a returned job ID',correct:true,explanation:'The status contract describes behaviour that integration tests can observe.'},{text:'The team prefers a particular cloud queue because it is familiar',correct:false,explanation:'A preference is a design option or decision, not a testable result.'},{text:'A failed job is attempted no more than three times',correct:true,explanation:'The bounded failure rule can be verified with a controlled test.'}]},
  {
    prompt:'Why should an assumption such as “the existing API authenticates clients” remain explicit?',options:[{text:'It records a dependency and tells the agent when to verify it or stop',correct:true,explanation:'Explicit uncertainty prevents an unverified dependency from silently becoming architecture.'},{text:'It proves authentication is correctly implemented, deployed, observed, and approved across every required environment',correct:false,explanation:'An assumption is not implementation or runtime evidence.'},{text:'It removes identity work from later security reviews because the design pack already records the expected boundary',correct:false,explanation:'The assumption still needs confirmation and does not replace review.'}]},
  {
    prompt:'Test and production both reference remote://platform/production Terraform state. What is the main design problem?',options:[{text:'The remote path is too long for Terraform and OpenTofu to read consistently from automated delivery runners',correct:false,explanation:'Path length is not the issue in this scenario.'},{text:'A test operation can read or modify production resource identity through a shared state boundary',correct:true,explanation:'Shared state joins environments that require separate planning, apply, migration, and recovery authority.'},{text:'A remote backend cannot preserve managed resource identifiers while two environment configurations are stored in Git',correct:false,explanation:'Resource identity is a normal state purpose when access and boundaries are correct.'}]},
  {
    prompt:'Select all items that must stay outside Terraform state for this queue feature.',multiSelect:true,options:[{text:'Queue resource ID used to manage the provisioned queue',correct:false,explanation:'Infrastructure identity is a normal part of Terraform state.'},{text:'Runtime job payload and status history',correct:true,explanation:'These values follow the application data lifecycle, not the infrastructure lifecycle.'},{text:'Reusable queue credential and encryption-key values',correct:true,explanation:'Secret management should own reusable secret values and rotation.'},{text:'Access-policy resource ID managed by Terraform',correct:false,explanation:'The managed policy identity belongs to the infrastructure control record.'}]},
  {
    prompt:'A worker replica count is reviewed as part of the deployed workload, while Git promotes the approved values revision. Which ownership is strongest?',options:[{text:'Helm owns the deploy-time replica setting; GitOps owns promotion of its reviewed revision',correct:true,explanation:'The setting and the environment promotion have related but distinct lifecycles.'},{text:'GitOps owns the setting and its promotion because every reviewed file eventually becomes part of runtime configuration',correct:false,explanation:'Promotion ownership does not replace the lifecycle owner of the setting.'},{text:'Terraform owns the replica setting and promotion because running additional pods consumes provisioned infrastructure capacity',correct:false,explanation:'Capacity impact does not make a workload replica setting an infrastructure resource.'}]},
  {
    prompt:'Select all lifecycle placements that are appropriate for the design pack.',multiSelect:true,options:[{text:'Terraform owns creation of the queue and its access-policy resources',correct:true,explanation:'These are infrastructure resources with a plan, apply, and state lifecycle.'},{text:'Application configuration owns retry count and allowed job-status transitions',correct:true,explanation:'These values define runtime product and failure behaviour.'},{text:'Secret management owns reusable credential values and rotation',correct:true,explanation:'The secret value needs a restricted lifecycle outside state and Git.'},{text:'Helm owns approval to promote a reviewed production revision',correct:false,explanation:'GitOps and its human review process own promotion; Helm renders deploy-time configuration.'}]},
  {
    prompt:'A Terraform plan adds one queue and reports no replacement. Why can the overall feature still need high review?',options:[{text:'Every additive resource becomes destructive when a later apply refreshes state and evaluates provider configuration',correct:false,explanation:'Additive and destructive are different classes, though a feature may contain both.'},{text:'The feature also adds permissions, trust crossings, cost, runtime data, and a later traffic migration',correct:true,explanation:'Risk review covers the whole dependency graph, not only Terraform action counts.'},{text:'A resource plan provides no useful review information until a production apply writes updated remote state',correct:false,explanation:'A plan is useful bounded evidence, but it does not describe every lifecycle.'}]},
  {
    prompt:'Which sequence is the safest rollback intent after clients have submitted asynchronous jobs?',options:[{text:'Delete the queue and result store, revert Git, restore the old route, and then inspect whether accepted jobs were lost',correct:false,explanation:'Infrastructure removal before data handling can destroy accepted work.'},{text:'Stop new submissions, drain or recover accepted work, restore the old route, then remove resources after approval',correct:true,explanation:'The order protects in-flight data and separates service rollback from destructive cleanup.'},{text:'Scale every worker to zero, keep accepting queued jobs, and wait for the next scheduled production deployment',correct:false,explanation:'The queue would continue to grow while processing remains unavailable.'}]},
  {
    prompt:'What is the strongest reason to write the queue ADR before implementation?',options:[{text:'It makes Terraform and OpenTofu validation pass without downloading providers or initializing a working directory',correct:false,explanation:'An ADR does not change Terraform command requirements.'},{text:'It exposes ownership trade-offs, rejected options, consequences, and rollback order while the design can still change',correct:true,explanation:'Reviewers can challenge the decision before generated code makes it costly to revise.'},{text:'It lets the coding agent record the architecture choice and approve the final decision without another reviewer',correct:false,explanation:'The agent may draft evidence, but named humans approve the design.'}]},
  {
    prompt:'Select all statements that correctly describe the CALM queue model.',multiSelect:true,options:[{text:'Nodes represent components such as the API, queue, worker, and result store',correct:true,explanation:'Nodes are the reviewable architecture building blocks at the chosen abstraction.'},{text:'Relationships record interactions and technical connections between node IDs',correct:true,explanation:'They turn a component list into a directional, queryable graph.'},{text:'Metadata can record ownership, version, review status, and trust boundary',correct:true,explanation:'Metadata adds organizational context to the architecture elements.'},{text:'A control entry proves the runtime system currently enforces the requirement',correct:false,explanation:'A control is a design requirement until implementation and runtime evidence prove enforcement.'}]},
  {
    prompt:'The unsafe starter reports “No issues found” from CALM validation. What is the correct conclusion?',options:[{text:'The JSON conforms to the pinned CALM schema, but ownership and state safety still need separate checks',correct:true,explanation:'Schema shape and engineering correctness are distinct evidence claims.'},{text:'The shared test and production state is safe because every model relationship uses a valid node identifier',correct:false,explanation:'Valid relationships do not approve unsafe state ownership.'},{text:'The architecture is already implemented, deployed, runtime-observed, and ready for a production approval decision',correct:false,explanation:'Schema validation says nothing about implementation, deployment, or approval.'}]},
  {
    prompt:'The CALM package cannot be downloaded during a learner run. How should the evidence record describe the result?',options:[{text:'CALM passed because a text editor can parse the architecture file as well-formed JSON without errors',correct:false,explanation:'JSON parsing is narrower than CALM schema validation.'},{text:'CALM failed because every npm registry or network error means the architecture model violates its pinned schema',correct:false,explanation:'Package availability and model validity are different conditions.'},{text:'CALM validation was not run; continue the local design check and preserve the missing standards evidence',correct:true,explanation:'This reports the network boundary accurately without inventing a result.'}]},
  {
    prompt:'Which review question is best answered by the small source-linked dependency graph?',options:[{text:'Which component relationships cross public, workload, and secret-management boundaries?',correct:true,explanation:'A bounded graph makes nodes, direction, interfaces, and trust crossings visible.'},{text:'Whether the deployed queue currently meets its measured latency and recovery service levels in production',correct:false,explanation:'That requires runtime telemetry from an implemented environment.'},{text:'Whether the accountable platform director accepts every remaining business, security, cost, and operational risk',correct:false,explanation:'A graph supports the decision but cannot make the accountable human judgment.'}]},
  {
    prompt:'Both local and CALM validators pass for the candidate design pack. What should happen next?',options:[{text:'Generate and apply the complete implementation because two independent automated validation gates now imply production approval',correct:false,explanation:'Validation does not grant implementation or deployment authority.'},{text:'Record the exact artifacts and evidence, then request the named platform, application, and security review',correct:true,explanation:'The candidate is ready for human decision while implementation remains blocked.'},{text:'Mark every declared architecture control as runtime-enforced and remove the pending approval status from the design pack',correct:false,explanation:'No implementation or runtime observation has occurred.'}]}
]} />
