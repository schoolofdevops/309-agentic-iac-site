---
sidebar_position: 5
title: 'Quiz: Deliver Infrastructure Safely with GitOps and Human Approval'
---

import Quiz from '@site/src/components/Quiz';

# Deliver Infrastructure Safely with GitOps and Human Approval Quiz

<Quiz questions={[
  {
    prompt:'An agent authored a Terraform and Helm change. Which identity should approve its production promotion?',
    options:[
      {text:'The delivery workflow identity that produced the plan evidence',correct:false,explanation:'The workflow produces bounded evidence but cannot provide accountable independent approval.'},
      {text:'An eligible human reviewer who inspected the exact revision and did not author its privilege change',correct:true,explanation:'Independent review prevents the authoring identity from granting and approving its own authority.'},
      {text:'A repository administrator who has not inspected the exact revision',correct:false,explanation:'Administrative access does not replace review of the commit, evidence, purpose, and target.'},
      {text:'The runtime platform owner after the change reaches the cluster',correct:false,explanation:'Approval must precede delivery and remain separate from the runtime identity.'},
    ]
  },
  {
    prompt:'Which repository controls help turn a pull request into an enforceable review boundary? Select all that apply.',
    multiSelect:true,
    options:[
      {text:'Path ownership that requests the correct specialist reviewers',correct:true,explanation:'CODEOWNERS can route Terraform, Helm, and workflow files to their responsible teams.'},
      {text:'Required checks bound to the current proposed commit',correct:true,explanation:'A later push should invalidate results that describe an earlier revision.'},
      {text:'A merge rule that trusts any green check name without binding it to the current head commit',correct:false,explanation:'A stale or look-alike check can describe different source and cannot authorize the current change.'},
      {text:'Protected branch rules that enforce eligible independent approval',correct:true,explanation:'The host must prevent the author from bypassing the required decision path.'},
    ]
  },
  {
    prompt:'A fork pull request changes its workflow to use pull_request_target and then executes the fork checkout. What is the main risk?',
    options:[
      {text:'The workflow can execute fork code safely because a production environment still requires later approval',correct:false,explanation:'An environment gate does not make earlier execution under a trusted base context safe.'},
      {text:'The base token remains safe because checkout names the full pull request SHA',correct:false,explanation:'Exact source identity does not remove the elevated event context or prevent hostile code execution.'},
      {text:'Untrusted fork code may execute with the more trusted base context and reach permissions unavailable to a normal fork job',correct:true,explanation:'That combination can expose elevated tokens or secrets to candidate-controlled code.'},
      {text:'The event is safe while the job creates only a plan and never applies it',correct:false,explanation:'Planning still executes candidate code and can expose tokens, secrets, or artifact contents.'},
    ]
  },
  {
    prompt:'Which controls belong in a bounded GitHub Actions plan workflow? Select all that apply.',
    multiSelect:true,
    options:[
      {text:'Explicit minimum token permissions for the plan job',correct:true,explanation:'Read-only planning should not inherit broad repository or deployment writes.'},
      {text:'A workspace-wide artifact upload because the later job will verify one file hash',correct:false,explanation:'A broad upload can disclose unrelated source, credentials, state, or sensitive plan data.'},
      {text:'An allow-listed artifact path with a recorded digest',correct:true,explanation:'The consumer needs bounded bytes and identity rather than a workspace archive.'},
      {text:'Executable third-party actions pinned to full commit SHAs',correct:true,explanation:'Exact pins prevent a mutable tag from silently selecting different code.'},
    ]
  },
  {
    prompt:'A reviewed Terraform plan names commit A, but the branch now points to commit B. What should the apply path do?',
    options:[
      {text:'Reject the stale plan and produce evidence for commit B',correct:true,explanation:'Approval for one source revision cannot authorize another unreviewed revision.'},
      {text:'Apply the old plan after confirming that branch ownership and the original author have not changed',correct:false,explanation:'Stable ownership does not make a plan describe new source, state, variables, or target facts.'},
      {text:'Update only the report source field and retain the approved binary plan',correct:false,explanation:'Editing metadata cannot make old plan bytes represent a new commit and current state.'},
      {text:'Ask the reviewer to approve the moving branch name instead of either commit',correct:false,explanation:'A branch name can move again and does not provide the exact subject required by approval.'},
    ]
  },
  {
    prompt:'Which fields help bind Terraform or OpenTofu plan evidence to its reviewed transaction? Select all that apply.',
    multiSelect:true,
    options:[
      {text:'Only a workflow URL and a successful plan check retained in the pull request history',correct:false,explanation:'Those facts do not bind source, bytes, actions, engine, target, or policy results.'},
      {text:'Source revision, workflow digest, engine, and exact version',correct:true,explanation:'These identify important code and execution inputs used to produce the plan.'},
      {text:'Plan digest, plan JSON digest, addresses, and resource actions',correct:true,explanation:'These bind the artifact and the semantic operations presented for review.'},
      {text:'Gate results, exact target identity, approval scope, and an explicit field that keeps apply permission closed',correct:true,explanation:'These show evaluator outcome and whether the artifact closes the apply path.'},
    ]
  },
  {
    prompt:'Terraform and OpenTofu produce different plan hashes but the same accepted resource actions. What should the reviewer do?',
    options:[
      {text:'Approve either plan by address alone because byte identity no longer matters',correct:false,explanation:'Addresses omit engine, plan bytes, action details, target identity, and policy context.'},
      {text:'Normalize both files into one shared hash before retaining the evidence',correct:false,explanation:'Replacing distinct artifacts hides a real compatibility and provenance fact.'},
      {text:'Reject both engines because equivalent actions must always create equal bytes',correct:false,explanation:'Engine and lock differences can change bytes while required semantic actions agree.'},
      {text:'Retain byte differences and compare the required semantic actions',correct:true,explanation:'Different engine artifacts can be honest while the reviewed operation set agrees.'},
    ]
  },
  {
    prompt:'Which statements correctly describe Argo CD status? Select all that apply.',
    multiSelect:true,
    options:[
      {text:'Synced means tracked live fields match desired state at that observation',correct:true,explanation:'It is a time-bound comparison and not a complete application test.'},
      {text:'Healthy proves that all external dependencies and user transactions completed successfully',correct:false,explanation:'Resource health does not observe every external service or application result.'},
      {text:'Operation phase reports the result of a requested sync action while remaining separate from later health and drift observations',correct:true,explanation:'Succeeded, Failed, or Error describes the operation and not all future state.'},
      {text:'OutOfSync identifies the person or controller that caused the detected difference',correct:false,explanation:'The status reports a difference; separate evidence must identify its source.'},
    ]
  },
  {
    prompt:'Argo reports Synced, Healthy, and Succeeded, but the application request fails. What is the correct conclusion?',
    options:[
      {text:'The client failure can be ignored after two independent controller status fields remain green',correct:false,explanation:'Controller observations do not replace an application transaction and its dependencies.'},
      {text:'Controller evidence passed while application correctness still failed',correct:true,explanation:'A separate runtime transaction is required beyond desired state and resource health.'},
      {text:'The delivery commit must be unsigned because the runtime result differs',correct:false,explanation:'Signature state and application behavior are separate evidence questions.'},
      {text:'Terraform should take ownership of the same Kubernetes workload for recovery',correct:false,explanation:'Adding a competing desired-state owner does not diagnose the failed request.'},
    ]
  },
  {
    prompt:'A workflow uploads a correctly hashed saved Terraform plan and plan JSON to a broadly readable artifact. What is the safest review decision?',
    options:[
      {text:'Accept it because the hash prevents readers from learning sensitive values',correct:false,explanation:'A digest detects changed bytes but provides no confidentiality for artifact contents.'},
      {text:'Retain the broad artifact briefly because terminal redaction also covers stored files',correct:false,explanation:'Saved plans and plan JSON can expose clear-text values that terminal output hides.'},
      {text:'Reject the broad upload, keep plan bytes in a protected artifact for the controlled consumer, and publish a sanitized review summary',correct:true,explanation:'The controlled consumer needs restricted access while reviewers need only bounded non-secret evidence.'},
      {text:'Publish only the binary plan because JSON is the only representation that can contain secrets',correct:false,explanation:'Both the saved binary plan and its JSON representation can carry sensitive data.'},
    ]
  },
  {
    prompt:'An Argo Application tracks targetRevision HEAD. Which controls keep promotion evidence bound? Select all that apply.',
    multiSelect:true,
    options:[
      {text:'Accept HEAD when the Application was Healthy before the repository refreshed',correct:false,explanation:'Earlier health does not bind a later moving symbolic revision to approval.'},
      {text:'Compare status.sync.revision with the exact human-approved full commit',correct:true,explanation:'The resolved revision shows which commit the observed sync actually used.'},
      {text:'Approve the branch name once and reuse that decision for later commits on the same branch',correct:false,explanation:'A branch can move, so later commits need their own evidence and approval.'},
      {text:'Use a protected promotion ref and bind it to an immutable workload artifact digest',correct:true,explanation:'Protected movement plus resolved commit and artifact identity creates reviewable promotion evidence.'},
    ]
  },
  {
    prompt:'Argo manages a Helm-rendered workload. An operator runs Helm rollback directly, but Git still names the newer version. What may happen?',
    options:[
      {text:'Argo can reconcile the workload back to the newer Git intent because the direct Helm rollback did not change desired state',correct:true,explanation:'A direct Helm action conflicts with the active Git desired-state owner.'},
      {text:'Helm can keep the old version only while Argo reports the Application as Healthy',correct:false,explanation:'Health does not transfer ownership or prevent the next Git reconciliation.'},
      {text:'The rollback becomes durable after a platform reviewer approves the live release state',correct:false,explanation:'Approval of live drift does not update the Git source that Argo continues to own.'},
      {text:'The direct rollback disables reconciliation until another Git commit is created',correct:false,explanation:'A Helm command does not suspend the Argo Application or its comparison loop.'},
    ]
  },
  {
    prompt:'A Terraform apply fails after one provider operation succeeds. Which next step is safest?',
    options:[
      {text:'Repeat the saved plan after confirming that its digest still matches the approved artifact',correct:false,explanation:'A matching digest does not account for remote and state changes made by the failed apply.'},
      {text:'Remove the completed address from state so the next run starts from the earlier model',correct:false,explanation:'Unreviewed state removal can create duplicate or unmanaged real infrastructure.'},
      {text:'Run a targeted apply for the failed address before refreshing the complete dependency graph',correct:false,explanation:'A target can omit dependencies and still relies on assumptions made before partial progress.'},
      {text:'Compare configuration, state, remote objects, and operation evidence',correct:true,explanation:'Current partial state determines whether refresh, import, repair, or roll forward is safe.'},
    ]
  },
  {
    prompt:'Which evidence links belong in a complete delivery audit trail? Select all that apply.',
    multiSelect:true,
    options:[
      {text:'Request, exact commit, required checks, and reviewer decision',correct:true,explanation:'These connect intent and source to evaluated human review.'},
      {text:'Approved artifact, purpose, target environment, and delivery result',correct:true,explanation:'These bind authorization to specific bytes, action, and destination.'},
      {text:'A controller summary retained without target revision, object identity, or observation time',correct:false,explanation:'Unbound status cannot show which source and runtime objects supported the claim.'},
      {text:'Runtime observation, recovery revision, and cleanup proof',correct:true,explanation:'These show observed effect, correction history, and final course-owned state.'},
    ]
  },
  {
    prompt:'The local learner replay peaked at 1.680 GiB on the named Kind node. How should that result be used?',
    options:[
      {text:'Report it as one measured workload observation below the 4 GiB ceiling',correct:true,explanation:'The value describes the exact named-node sampling boundary on the tested host.'},
      {text:'Use it as a safe production limit because the application request completed successfully',correct:false,explanation:'A deterministic local request does not model production scale, traffic, or resilience.'},
      {text:'Treat it as total host demand after adding the configured Docker capacity to the sample',correct:false,explanation:'Configured capacity and named-container working set are different measurements.'},
      {text:'Reject learners below that value before checking whether the smaller profile can proceed',correct:false,explanation:'The course reference is warn-only and one observed peak is not a rejection threshold.'},
    ]
  },
]} />
